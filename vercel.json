{
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @secure-room/api-contract build && pnpm --filter client build",
  "outputDirectory": "apps/client/dist",
  "framework": "vite"
}
